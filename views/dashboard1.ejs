<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="output.css">
</head>
<body class="flex justify-center bg-gray-200">
  <div class="w-2/3 p-8 border-spacing-6 shadow-xl rounded-2xl m-16 bg-white flex justify-center flex-col">
   <h2 class="text-center text-3xl font-bold p-4">Welcome, <%= user.name %>!</h2>
   <div class="flex justify-center">
   <% if (user.profilePicture) { %>
        <img src="<%= user.profilePicture %>" alt="Profile Picture" class="w-24 h-24 p-1 rounded-full border-4 border-black align-middle">
      <% } else { %>
        <p>No profile picture uploaded.</p>
      <% } %>
    </div>
    <p class="text-center p-2 text-xl">You are now logged in.</p>
    <div class="flex justify-center p-3">
      <button  class=" bg-blue-400 h-10 text-center my-5 w-16 rounded-xl m-3 text-white"><a href="/">Log out</a></button>
     <button class=" bg-blue-400 h-10 text-center my-5 w-28 rounded-xl text-white"><a href="/users/update/<%= user._id %>" >Edit Profile</a></button>
     <button class=" bg-blue-400 h-10 text-center my-5 w-28 rounded-xl text-white"><a href="/chat/messages/6773b882b3c2dd227140ef52?token=<%= token %>" >Chat with ur Friend</a></button>
     <button class=" bg-blue-400 h-10 text-center my-5 w-28 rounded-xl text-white"><a href="/friend/<%= user._id %>?token=<%= token %>" >View Friends</a></button>

    </div>
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="output.css">
</head>
<body class="bg-gray-200">
    <!-- Navbar -->
    <nav class="bg-blue-600 p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Left side: Profile Picture and Name -->
            <div class="flex items-center space-x-4">
                <% if (user.profilePicture) { %>
                    <img src="<%= user.profilePicture %>" alt="Profile Picture" class="w-12 h-12 rounded-full border-2 border-white">
                <% } else { %>
                    <div class="w-12 h-12 rounded-full bg-gray-300 flex items-center justify-center text-blue-600 font-bold text-lg">
                        <%= user.name.charAt(0).toUpperCase() %>
                    </div>
                <% } %>
                <span class="text-white font-semibold text-xl"><%= user.name %></span>
            </div>
            
            <!-- Right side: Navbar Links -->
            <div class="space-x-4">
                <a href="/" class="text-white hover:text-blue-200">Log out</a>
                <a href="/users/update/<%= user._id %>" class="text-white hover:text-blue-200">Edit Profile</a>
                <a href="/chat/messages/6773b882b3c2dd227140ef52?token=<%= token %>" class="text-white hover:text-blue-200">Chat with Friend</a>
                <a href="/friend/<%= user._id %>?token=<%= token %>" class="text-white hover:text-blue-200">View Friends</a>
            </div>
        </div>
    </nav>

    <!-- Main Content (optional) -->
    <main class="container mx-auto p-8">
        <h2 class="text-center text-3xl font-bold p-4">Welcome, <%= user.name %>!</h2>
        <p class="text-center p-2 text-xl">You are now logged in.</p>
    </main>
</body>
</html>
